## 更新记录

- 2025-12-28：创建 `auto-tauri` 项目并编写 `plan.md` 开发计划。
- 2025-12-28：搭建基础目录结构，新增 `sidecar/` 与 `server/` 目录，规划 `desktop/` 作为 Tauri 桌面端入口。
- 2025-12-28：使用 `create-tauri-app` 在 `desktop/` 目录初始化 Tauri（React + TS + Vite）。
 - 2025-12-28：在 `server/` 下初始化 Go 模块，接入 Gin + GORM，并通过 env 文件配置 PostgreSQL 连接，添加基础数据模型和 `/health` 接口。
- 2025-12-28：在 `server` 中实现 `POST /api/v1/auth/register`、`POST /api/v1/auth/login`（JWT）、`POST /api/v1/credits/recharge` 与 `POST /api/v1/tasks/start` 的接口骨架和事务扣费逻辑。
- 2025-12-28：在 `server` 中新增 `GET /api/v1/auth/me` 接口，用于返回当前用户信息，并在 `desktop` 中替换默认示例页面，接入注册、登录、积分充值与任务启动的前端界面，使用本地存储保存 JWT，并通过环境变量支持后端地址配置。
 - 2025-12-28：在 `server` 中集成 Redis 客户端，通过用户级 Redis 锁保护积分充值与任务扣费的原子性，更新 `.env.example` 和 `plan.md` 以反映配置与设计。
- 2025-12-30：完成 Sidecar 基础建设：
  - 初始化 `sidecar` 目录，创建基于 Node.js + TypeScript 的 CLI 工具。
  - 实现 Sidecar 与主进程的通信协议：通过 stdio (stdin/stdout/stderr) 接收 JSON 任务指令并流式输出日志与结果。
  - 使用 `pkg` 将 Sidecar 打包为独立二进制文件 (`hyperagent`)，适配 macOS arm64 架构。
- 2025-12-30：完成 Tauri 与 Sidecar 的集成：
  - 配置 `tauri.conf.json` 注册 externalBin。
  - 配置 `capabilities` 允许 `shell:allow-execute` 权限。
  - 在前端 `App.tsx` 中引入 `@tauri-apps/plugin-shell`，实现「扣费成功 -> 启动 Sidecar -> 实时展示日志 -> 展示结果」的完整闭环。
  - 修复 Tauri 构建问题：将 Sidecar 二进制文件移动至 `src-tauri/` 根目录以确保构建脚本正确识别 `externalBin`。
- 2025-12-30：实现 Auto Agents 功能：
  - 新增 `sidecar/src/auto_agents` 模块，集成 Playwright 自动化逻辑。
  - 实现「访问 URL -> 截图 -> 自动发布至小红书」的完整链路（需用户手动扫码/登录）。
  - 更新前端界面，支持输入目标 URL 触发自动化任务。
- 2025-12-30：集成 Dashboard 样式：
  - 将 `example_dashboard/code.html` 的 Tailwind 配置和样式迁移至 `desktop` 项目。
  - 配置 `tailwind.config.js`、`postcss.config.js` 和 `src/index.css`。
  - 重构前端 `App.tsx`，实现基于 Tailwind 的 Dashboard 布局（侧边栏、顶部导航、统计卡片），并保留原有的认证与任务功能。
- 2025-12-30：修复后端 CORS 问题：
  - 在 Gin 服务中添加 CORS 中间件，处理 `OPTIONS` 预检请求，解决前端调用 API 时的 404 错误。
- 2025-12-30：国际化（i18n）更新：
  - 将整个项目的用户界面语言（React 前端、Sidecar 日志、Tauri 窗口标题）全部修改为中文，提升中文用户体验。
  - 优化登录/注册页面样式，将输入框的占位符文字（placeholder）调整为统一的浅灰色（使用 hex 值确保生效：`#d1d5db` / `#9ca3af`）。
  - 深入中文化：将登录页剩余的英文（Sign In, Sign Up 等）全部翻译，并将 "TaskMaster" 品牌名统一修改为 "任务大师"（包括窗口标题）。
- 2025-12-30：功能更新：
  - 登录页新增“自动登录”功能，用户可勾选以记住登录状态。
  - 优化 Token 存储逻辑：仅当用户勾选“自动登录”时才将 Token 持久化至本地存储。
  - 界面布局重构：
    - 移除仪表盘概览标题（H2）和欢迎语（P）。
    - 将“充值”功能整合至“总余额”卡片中，优化空间利用。
    - 将“开始新任务”卡片移至仪表盘右上角，与统计卡片并列展示。
    - 优化“开始任务”交互：将任务输入表单移入弹窗（Modal），右上角仅保留醒目的“开始新任务”按钮，提升界面整洁度。
  - 功能增强：
    - 任务启动新增“自动工作流”与“网页获取”两种模式选择。
    - 后端数据库与 API 同步升级，支持任务类型（Type）字段的存储与处理。
    - Sidecar 逻辑更新，支持通用“网页获取”模式的调度。

- 2025-12-31：架构重大重构 - 从“任务中心”转向“项目中心”：
  - **业务逻辑修正**：明确了「项目 (Project)」作为自动化模板（URL/Prompt 配置），「任务 (Task)」作为项目执行实例的关系。
  - **后端模型升级**：新增 `Project` 模型，重构 `Task` 模型关联 `project_id`。
  - **API 扩展**：
    - 实现项目 CRUD：`POST /projects`, `GET /projects`, `DELETE /projects/:id`。
    - 实现项目执行接口：`POST /projects/:id/execute`，包含余额检查、扣费、创建任务记录。
  - **前端 UI 重构**：
    - 仪表盘新增「新建项目」入口及最近项目快速启动。
    - 新增「项目管理」视图，支持项目查看、启动、删除。
    - 完善执行流：点击启动 -> 后端生成任务 -> 跳转执行详情页 -> 实时日志流。
    - 更新「任务历史」列表，展示所有项目执行的历史记录。
  - **文档更新**：同步更新 `plan.md`，纠正逻辑偏差。
