# 知乎热门AI文章定时抓取测试计划

## 测试策略概述
本测试计划针对知乎热门AI文章定时抓取系统进行全面测试，确保各模块功能正常、模块间协作顺畅、系统稳定运行。

## 1. Unit Tests (单元测试)

### 1.1 知乎爬虫模块测试 (zhihu_crawler.py)

#### 测试函数列表：
- `get_page_content(url)` - 页面内容获取
- `parse_article_list(html)` - 文章列表解析
- `extract_article_info(article_element)` - 文章信息提取
- `handle_request_error(response)` - 请求错误处理

#### Mock 数据准备：
```python
# Mock HTML 响应数据
mock_html = """
<html>
    <body>
        <div class="ContentItem">
            <h2><a href="/article/123">AI技术发展趋势</a></h2>
            <div class="ContentItem-meta">
                <span class="UserLink">张三</span>
                <span>2024-01-20</span>
            </div>
            <p class="ContentItem-excerpt">本文讨论了AI技术的发展趋势...</p>
        </div>
        <div class="ContentItem">
            <h2><a href="/article/456">机器学习入门指南</a></h2>
            <div class="ContentItem-meta">
                <span class="UserLink">李四</span>
                <span>2024-01-19</span>
            </div>
            <p class="ContentItem-excerpt">机器学习入门教程...</p>
        </div>
    </body>
</html>
"""

# Mock 错误响应数据
mock_error_responses = [
    {"status_code": 404, "text": "Not Found"},
    {"status_code": 403, "text": "Forbidden"},
    {"status_code": 500, "text": "Internal Server Error"},
    {"status_code": None, "text": "Connection Error"}
]
```

#### 测试用例：
1. **测试页面内容获取**
   - 正常响应测试
   - 网络超时测试
   - HTTP错误状态测试
   - 响应内容为空测试

2. **测试文章列表解析**
   - 标准HTML结构解析
   - 空页面解析
   - 异常HTML结构解析
   - 无文章内容解析

3. **测试文章信息提取**
   - 完整文章信息提取
   - 缺失字段处理
   - 特殊字符处理
   - 链接格式验证

### 1.2 Excel操作模块测试 (excel_handler.py)

#### 测试函数列表：
- `create_excel_file(filepath)` - 创建Excel文件
- `write_articles_to_excel(articles, filepath)` - 写入文章数据
- `read_excel_data(filepath)` - 读取Excel数据
- `update_excel_data(articles, filepath)` - 更新Excel数据

#### Mock 数据准备：
```python
# Mock 文章数据
mock_articles = [
    {
        "title": "AI技术发展趋势",
        "url": "https://zhihu.com/article/123",
        "author": "张三",
        "publish_time": "2024-01-20",
        "excerpt": "本文讨论了AI技术的发展趋势..."
    },
    {
        "title": "机器学习入门指南", 
        "url": "https://zhihu.com/article/456",
        "author": "李四",
        "publish_time": "2024-01-19",
        "excerpt": "机器学习入门教程..."
    }
]
```

#### 测试用例：
1. **测试Excel文件创建**
   - 正常文件创建
   - 目录不存在时创建
   - 文件权限问题测试

2. **测试数据写入**
   - 正常数据写入
   - 空数据写入
   - 大数据量写入
   - 中文数据写入

3. **测试数据读取**
   - 正常数据读取
   - 空文件读取
   - 格式错误文件读取

### 1.3 数据处理模块测试 (data_processor.py)

#### 测试函数列表：
- `clean_text(text)` - 文本清洗
- `format_datetime(datetime_str)` - 时间格式化
- `remove_duplicates(articles)` - 去重处理
- `validate_article_data(article)` - 数据验证

#### 测试用例：
1. **测试文本清洗**
   - 正常文本清洗
   - 空白字符处理
   - HTML标签清理
   - 特殊字符处理

2. **测试时间格式化**
   - 标准格式转换
   - 异常时间格式处理
   - 边界值测试

3. **测试去重处理**
   - 重复数据识别
   - 去重算法正确性
   - 大量数据处理性能

### 1.4 定时任务调度器测试 (scheduler.py)

#### 测试函数列表：
- `setup_daily_schedule()` - 设置定时任务
- `run_scheduled_task()` - 执行定时任务
- `check_schedule_status()` - 检查任务状态

#### 测试用例：
1. **测试定时任务设置**
   - 正常任务调度
   - 时间冲突处理
   - 任务参数验证

2. **测试任务执行**
   - 正常任务执行
   - 任务执行失败处理
   - 并发任务处理

### 1.5 配置文件测试 (config.py)

#### 测试用例：
1. **配置参数加载**
   - 正常配置文件读取
   - 配置文件不存在处理
   - 参数格式错误处理

2. **配置参数验证**
   - 必填参数检查
   - 参数类型验证
   - 参数范围验证

### 1.6 日志系统测试 (logger.py)

#### 测试用例：
1. **日志记录功能**
   - 不同级别日志记录
   - 日志格式正确性
   - 文件写入权限

2. **日志轮转功能**
   - 日志文件大小控制
   - 历史日志清理

## 2. Integration Tests (集成测试)

### 2.1 模块协作测试

#### 测试场景1：完整抓取流程
1. 配置参数设置
2. 执行单次抓取任务
3. 数据处理和清洗
4. Excel文件生成
5. 日志记录验证

**验证点：**
- 数据完整性检查
- 文件生成正确性
- 错误处理机制
- 性能指标监控

#### 测试场景2：定时任务流程
1. 定时任务启动
2. 任务执行监控
3. 数据增量更新
4. 异常情况处理

**验证点：**
- 定时任务准确性
- 数据去重正确性
- 系统稳定性
- 资源占用情况

#### 测试场景3：错误恢复流程
1. 网络连接中断
2. 网站结构变更
3. 文件写入失败
4. 系统恢复能力

**验证点：**
- 错误检测能力
- 自动重试机制
- 数据一致性
- 系统可恢复性

### 2.2 数据流测试

#### 测试数据流：
知乎页面 → 爬虫模块 → 数据处理 → Excel文件 → 定时调度

**验证点：**
- 数据格式一致性
- 转换逻辑正确性
- 性能损耗评估
- 内存使用情况

## 3. Manual Verification (手动验证)

### 3.1 环境准备检查
```bash
# 1. 验证Python环境
python --version

# 2. 验证依赖包安装
pip list | grep -E "(requests|beautifulsoup4|openpyxl|schedule)"

# 3. 验证项目目录结构
ls -la /path/to/project/
```

### 3.2 功能验证步骤

#### 3.2.1 基础功能验证
```bash
# 1. 手动执行爬虫模块
python -m zhihu_crawler --test

# 2. 验证Excel文件生成
ls -la output/
file output/articles_*.xlsx

# 3. 检查日志文件
tail -f logs/crawler.log
```

#### 3.2.2 定时任务验证
```bash
# 1. 测试定时任务设置
python scheduler.py --setup-test

# 2. 验证任务调度
python -c "import schedule; schedule.jobs"

# 3. 手动触发定时任务
python main.py --run-once
```

#### 3.2.3 配置文件验证
```bash
# 1. 验证配置文件语法
python -c "import config; print('Config OK')"

# 2. 检查配置参数
python main.py --show-config
```

### 3.3 性能验证
```bash
# 1. 监控内存使用
top -p $(pgrep -f "main.py")

# 2. 监控网络请求
netstat -an | grep :80

# 3. 检查文件大小
du -sh output/
du -sh logs/
```

### 3.4 错误场景模拟

#### 3.4.1 网络异常测试
```bash
# 1. 断网环境测试
# 拔掉网线后执行
python main.py --test-offline

# 2. 限速环境测试
# 使用tc命令限制带宽后执行
python main.py --test-slow-network
```

#### 3.4.2 权限异常测试
```bash
# 1. 无写入权限测试
chmod 000 output/
python main.py --test-no-permission

# 2. 磁盘空间不足测试
# 模拟磁盘满情况后执行
python main.py --test-no-space
```

## 4. Success Criteria (成功标准)

### 4.1 功能性成功标准

#### 核心功能验证
- [ ] 知乎热门AI文章页面能够成功访问和解析
- [ ] 文章信息（标题、链接、作者、发布时间）提取准确率 ≥ 95%
- [ ] Excel文件能够正确生成，格式规范，数据完整
- [ ] 定时任务能够按设定时间准确执行（误差 ≤ 30秒）
- [ ] 数据去重机制工作正常，无重复文章记录
- [ ] 日志系统记录完整，包含执行状态和错误信息

#### 错误处理验证
- [ ] 网络异常情况下程序不崩溃，具备自动重试机制
- [ ] 网站结构变更时能够检测并报告异常
- [ ] 文件写入失败时能够正确处理和告警
- [ ] 配置参数错误时程序启动时能够检测并提示
- [ ] 内存和磁盘资源不足时能够优雅降级

### 4.2 性能成功标准

#### 响应时间指标
- [ ] 单次页面抓取时间 ≤ 5秒
- [ ] 10篇文章数据处理时间 ≤ 2秒
- [ ] Excel文件生成时间 ≤ 1秒
- [ ] 定时任务整体执行时间 ≤ 10秒

#### 资源使用指标
- [ ] 内存使用峰值 ≤ 100MB
- [ ] CPU使用率平均 ≤ 50%
- [ ] 单次任务网络流量 ≤ 1MB
- [ ] 日志文件日增长 ≤ 10MB

#### 稳定性指标
- [ ] 连续运行24小时无内存泄漏
- [ ] 连续执行100次任务无崩溃
- [ ] 数据完整性验证100%通过
- [ ] 定时任务准确率 ≥ 99.5%

### 4.3 代码质量成功标准

#### 测试覆盖率
- [ ] 单元测试覆盖率 ≥ 80%
- [ ] 关键业务逻辑测试覆盖率 ≥ 90%
- [ ] 异常处理代码测试覆盖率 ≥ 70%
- [ ] 集成测试覆盖主要业务场景

#### 代码规范
- [ ] 通过Python代码风格检查（flake8/pycodestyle）
- [ ] 通过静态代码分析（pylint）
- [ ] 核心函数有完整的docstring文档
- [ ] 变量和函数命名符合规范

### 4.4 用户体验成功标准

#### 易用性验证
- [ ] 程序安装和配置步骤清晰，文档完整
- [ ] 错误信息友好，能够指导用户解决问题
- [ ] 程序执行状态可见，进度显示友好
- [ ] 支持命令行参数灵活配置

#### 维护性验证
- [ ] 日志信息详细，便于问题定位
- [ ] 配置文件结构清晰，易于修改
- [ ] 代码模块化良好，便于扩展
- [ ] 提供健康检查和维护命令

### 4.5 验收测试清单

#### 基础功能验收
1. **爬虫功能验收**
   - [ ] 能够成功访问知乎热门AI页面
   - [ ] 文章信息提取准确（人工抽样检查10篇文章）
   - [ ] 支持增量更新，不重复抓取
   - [ ] 反爬虫机制有效，请求频率合理

2. **数据存储验收**
   - [ ] Excel文件格式标准，列标题清晰
   - [ ] 数据类型正确（文本、日期、链接等）
   - [ ] 支持中文数据，无乱码问题
   - [ ] 文件权限正确，可读写

3. **定时任务验收**
   - [ ] 任务调度准确，延迟在可接受范围
   - [ ] 任务状态监控完整
   - [ ] 支持手动触发和自动执行
   - [ ] 任务失败告警机制有效

#### 压力测试验收
1. **并发测试**
   - [ ] 支持多实例同时运行
   - [ ] 线程/进程安全性验证
   - [ ] 资源竞争处理正确

2. **长时间运行测试**
   - [ ] 连续运行7天无问题
   - [ ] 内存使用稳定，无泄漏
   - [ ] 日志文件正常轮转
   - [ ] 定时任务依然准确

#### 安全测试验收
1. **网络安全**
   - [ ] HTTPS连接验证
   - [ ] 请求头设置合理
   - [ ] 异常请求处理正确

2. **数据安全**
   - [ ] 敏感信息不记录到日志
   - [ ] 文件访问权限控制
   - [ ] 数据传输加密

## 5. 测试执行计划

### 5.1 测试阶段安排

#### 阶段1：单元测试 (2天)
- 各模块函数级测试
- Mock数据测试
- 边界条件测试

#### 阶段2：集成测试 (2天)
- 模块间协作测试
- 数据流测试
- 端到端流程测试

#### 阶段3：系统测试 (3天)
- 完整系统功能测试
- 性能测试
- 稳定性测试

#### 阶段4：验收测试 (2天)
- 用户验收测试
- 文档验收
- 部署验证

### 5.2 风险评估

#### 高风险项
- 知乎网站反爬虫策略升级
- 网站结构变更导致解析失败
- 第三方库版本兼容性

#### 中风险项
- 网络环境不稳定
- 服务器性能瓶颈
- 数据量增长导致的性能问题

#### 缓解措施
- 建立监控告警机制
- 准备应急回滚方案
- 定期更新和维护

## 6. 测试工具和资源

### 6.1 测试工具
- pytest - 单元测试框架
- unittest.mock - 模拟对象
- coverage.py - 测试覆盖率
- memory_profiler - 内存性能分析
- requests-mock - HTTP请求模拟

### 6.2 测试环境
- Python 3.8+ 开发环境
- 测试用知乎账户
- 独立的测试数据目录
- 测试配置文件

### 6.3 人力资源
- QA测试工程师 × 2
- 开发工程师 × 1（支持）
- 测试执行时间：约9个工作日

---

**测试计划版本**: v1.0  
**制定日期**: 2024-01-25  
**QA负责人**: 待确认  
**下次评审**: 开发完成后